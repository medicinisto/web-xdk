/*
 * Import Everything in Layer.Core for running in React Native (never tested in React Native or in a browser)
 */
import '../utils/native-support/react-native';
import Core from './index';

module.exports = Core;
